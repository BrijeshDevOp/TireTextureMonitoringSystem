# Ignore everything by default
/*

# Keep the gitignore itself
!.gitignore

# Whitelist only these paths/files to be tracked
!/templates/
!/static/
!/app.py
!/model3.hdf5
!/requirements.txt
!/runtime.txt
!/README.md
!/DEPLOYMENT.md

# Explicitly ignore these files (already covered by /*, but listed for clarity)
/app_test.py
/.gitattributes
/model.ipynb
/upload.png
/dataset

# Ensure contents inside whitelisted directories are also included
!/templates/**
!/static/**


